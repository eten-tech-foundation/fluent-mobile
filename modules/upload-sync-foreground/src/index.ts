/**
 * Local Expo module — native Android entry is `UploadSyncForegroundModule.kt`.
 * App code loads it optionally via `requireOptionalNativeModule('UploadSyncForeground')`
 * (see `src/services/uploadSyncForeground.ts`) so Jest / non-prebuilt builds
 * degrade gracefully.
 */
export {};
