# /open-pr — alias of /create-pr

Do **not** duplicate instructions here.

Read and **fully execute** `.claude/commands/create-pr.md`.

Delivery-only (branch already has commits). For issue → branch → implement →
PR, use **`/start-issue`**.
