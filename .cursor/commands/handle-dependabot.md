# Handle Dependabot (Cursor)

Cursor entry point — **do not duplicate instructions here.**

Read and follow the canonical command:

**`.claude/commands/handle-dependabot.md`**

Triage, rebase, validate, and squash-merge **existing** Dependabot PRs one at
a time. Never push agent-authored commits to `main`.
