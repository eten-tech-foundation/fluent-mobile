# Open PR (Cursor)

Cursor entry point — **do not duplicate instructions here.**

`/open-pr` is an **alias** of `/create-pr`. Read and follow:

**`.claude/commands/create-pr.md`**

Delivery-only (branch already has commits). For issue → branch → implement →
PR, use **`/start-issue`** instead.
