# Dep Bump (Cursor)

Cursor entry point — **do not duplicate instructions here.**

Read and follow the canonical command:

**`.claude/commands/dep-bump.md`**

Expo-correct dependency add/bump/align (`npx expo install`, doctor, optional
prebuild). Hand off to `/create-pr` for a ticketed PR.
